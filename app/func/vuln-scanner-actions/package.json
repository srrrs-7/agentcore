{
  "name": "@app/vuln-scanner-actions",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/index.ts --outdir=dist --target=node --format=esm",
    "check:type": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@packages/logger": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.3",
    "vitest": "^4.0.18"
  }
}
