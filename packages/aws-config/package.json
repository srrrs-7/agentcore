{
  "name": "@packages/aws-config",
  "version": "0.0.0",
  "private": true,
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "check:type": "tsgo --noEmit",
    "test": "bun test --watch",
    "clean": "echo 'No build artifacts to clean'",
    "build": "echo 'No build step needed (TypeScript source directly used)'"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^25.2.2"
  }
}
